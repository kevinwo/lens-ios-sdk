import SwiftUI

struct CreateProfileView: View {
    @ObservedObject var viewModel = CreateProfileViewModel()

    var body: some View {
        Text("Create Profile View TBD")
    }
}

struct CreateProfileView_Previews: PreviewProvider {
    static var previews: some View {
        CreateProfileView()
    }
}
