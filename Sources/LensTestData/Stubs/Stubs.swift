#if DEBUG
import Foundation

/// Swift Packages appear to have an issue with loading resources correctly for a test bundle. Therefore, as a temporary message, will load JSON strings via extension stub functions.
public enum Stubs {}
#endif
